{% for reservations in reservations %}
            {
              "hostname": "{{ reservations.label | replace('-nmn', '') }}",
              "hw-address": "{{ reservations.mac }}",
              "ip-address": "{{ reservations.address }}"
{% if not loop.last %}
            },
{% else %}
            }
{% endif %}
{% endfor %}
