@Library('dst-shared@master') _

dockerBuildPipeline {
    repository = "cray"
    imagePrefix = "cray"
    app = "dhcp-kea-init"
    name = "dhcp-kea-init"
    description = "Cray service cray-dhcp-kea-init"
    dockerfile = "Dockerfile.dhcp-kea-init"
    product = "shasta-premium,shasta-standard"
}