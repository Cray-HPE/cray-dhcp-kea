{% for reservations in reservations %}
            {
              "hostname": "{{ reservations.label | replace('-nmn', '') }}",
              "mac": "{{ reservations.mac }}",
              "ip": "{{ reservations.address }}"
{% if not loop.last %}
            },
{% else %}
            }
{% endif %}
{% endfor %}
