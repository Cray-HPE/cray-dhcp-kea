@Library('dst-shared@master') _

dockerBuildPipeline {
    repository = "cray"
    imagePrefix = "cray"
    app = "dhcp"
    name = "dhcp"
    description = "Cray service cray-dhcp"
    dockerfile = "Dockerfile.dhcp"
    product = "shasta-premium,shasta-standard"
}
